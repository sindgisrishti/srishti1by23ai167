#include<stdio.h>
int main()
{
	int a[10], i; 
 	for (i=0; i<n; i++)
 	{
	 	printf("Enter the ten numbers of array\n");
  		scanf("%d", &a[i]);
 	}
 	for (i=0; i<n; i++)
 	{
  		printf("The ten numbers of array are:\n");
  		printf("%d\n", a[i]);
 	}
   	return 0;
} 